# 用户认证系统 — 实现计划

> **For agentic workers:** 使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 来逐任务实现此计划。

**目标：** 为知识库问答系统添加 FastAPI OAuth2 + JWT 用户认证，支持 admin/user 两种角色

**架构：** SQLite 存用户表 + bcrypt 密码哈希 + JWT token（HS256） + FastAPI OAuth2PasswordBearer 依赖注入

**技术栈：** FastAPI OAuth2, PyJWT, bcrypt, SQLite (sqlite3), Python 3.11

---

## 文件映射

| 文件 | 职责 | 操作 |
|------|------|------|
| `backend/auth/database.py` | SQLite 连接, 用户表创建, 默认 admin 初始化 | 新建 |
| `backend/auth/security.py` | bcrypt 密码哈希, JWT 生成/验证 | 新建 |
| `backend/auth/models.py` | User, Token, LoginRequest Pydantic 模型 | 新建 |
| `backend/auth/dependencies.py` | get_current_user, require_admin FastAPI 依赖 | 新建 |
| `backend/auth/__init__.py` | 空文件 | 新建 |
| `backend/api/auth.py` | POST /api/auth/login 端点 | 新建 |
| `backend/config.py` | 添加 JWT 配置项 | 修改 |
| `backend/main.py` | lifespan 中调用 init_admin_user() | 修改 |
| `backend/api/query.py` | 查询端点添加认证依赖 | 修改 |
| `backend/api/admin.py` | 管理端点添加 admin 权限依赖 | 修改 |
| `tests/test_auth.py` | 认证功能单元测试 | 新建 |

---

### Task 1: 创建数据库模块

**Files:**
- Create: `backend/auth/__init__.py`
- Create: `backend/auth/database.py`

- [ ] **Step 1: 创建空 __init__.py**

```bash
echo "" > "C:/Users/14120/enterprise-kb-agent/backend/auth/__init__.py"
```

- [ ] **Step 2: 创建 database.py**

```python
"""SQLite 数据库连接 + 用户表管理"""
import sqlite3
import os
from pathlib import Path
from loguru import logger

DB_DIR = Path(__file__).resolve().parent.parent.parent / "data"
DB_PATH = str(DB_DIR / "users.db")


def get_connection() -> sqlite3.Connection:
    """获取数据库连接"""
    DB_DIR.mkdir(parents=True, exist_ok=True)
    conn = sqlite3.connect(DB_PATH)
    conn.row_factory = sqlite3.Row
    conn.execute("PRAGMA journal_mode=WAL")
    return conn


def create_users_table():
    """创建用户表（如果不存在）"""
    conn = get_connection()
    conn.execute("""
        CREATE TABLE IF NOT EXISTS users (
            id INTEGER PRIMARY KEY AUTOINCREMENT,
            username TEXT UNIQUE NOT NULL,
            password_hash TEXT NOT NULL,
            role TEXT NOT NULL DEFAULT 'user',
            created_at TEXT NOT NULL DEFAULT (datetime('now'))
        )
    """)
    conn.commit()
    conn.close()


def init_admin_user():
    """首次启动时创建默认管理员账号"""
    create_users_table()
    conn = get_connection()
    row = conn.execute("SELECT COUNT(*) as cnt FROM users").fetchone()
    if row["cnt"] == 0:
        from .security import hash_password
        pw_hash = hash_password("admin123")
        conn.execute(
            "INSERT INTO users (username, password_hash, role) VALUES (?, ?, ?)",
            ("admin", pw_hash, "admin"),
        )
        conn.commit()
        logger.warning("=" * 50)
        logger.warning("默认管理员账号已创建: admin / admin123")
        logger.warning("请尽快修改密码!")
        logger.warning("=" * 50)
    conn.close()
```

- [ ] **Step 3: 验证 import**

```bash
cd C:/Users/14120/enterprise-kb-agent && D:/Anaconda3/python.exe -c "from backend.auth.database import get_connection, create_users_table, init_admin_user; print('OK')"
```

预期：`OK`

- [ ] **Step 4: Commit**

```bash
cd "C:\Users\14120\enterprise-kb-agent"
git add backend/auth/__init__.py backend/auth/database.py
git commit -m "feat: add SQLite user database module"
```

---

### Task 2: 创建安全模块（bcrypt + JWT）

**Files:**
- Create: `backend/auth/security.py`

- [ ] **Step 1: 创建 security.py**

```python
"""密码哈希 + JWT token 生成/验证"""
import bcrypt
import jwt
from datetime import datetime, timedelta, timezone
from ..config import JWT_SECRET, JWT_ALGORITHM, JWT_EXPIRE_MINUTES


def hash_password(password: str) -> str:
    """bcrypt 哈希密码"""
    salt = bcrypt.gensalt()
    return bcrypt.hashpw(password.encode("utf-8"), salt).decode("utf-8")


def verify_password(plain_password: str, hashed_password: str) -> bool:
    """验证密码是否匹配"""
    return bcrypt.checkpw(
        plain_password.encode("utf-8"),
        hashed_password.encode("utf-8"),
    )


def create_access_token(data: dict) -> str:
    """生成 JWT access token"""
    to_encode = data.copy()
    expire = datetime.now(timezone.utc) + timedelta(minutes=JWT_EXPIRE_MINUTES)
    to_encode.update({"exp": expire})
    return jwt.encode(to_encode, JWT_SECRET, algorithm=JWT_ALGORITHM)


def decode_access_token(token: str) -> dict | None:
    """解码 JWT token，失败返回 None"""
    try:
        return jwt.decode(token, JWT_SECRET, algorithms=[JWT_ALGORITHM])
    except jwt.PyJWTError:
        return None
```

- [ ] **Step 2: 验证 import**

```bash
cd C:/Users/14120/enterprise-kb-agent && D:/Anaconda3/python.exe -c "
from backend.auth.security import hash_password, verify_password, create_access_token, decode_access_token
pw = hash_password('test123')
assert verify_password('test123', pw)
assert not verify_password('wrong', pw)
token = create_access_token({'sub': 'admin'})
decoded = decode_access_token(token)
assert decoded['sub'] == 'admin'
assert decode_access_token('bad.token.here') is None
print('ALL OK')
"
```

预期：`ALL OK`

- [ ] **Step 3: Commit**

```bash
cd "C:\Users\14120\enterprise-kb-agent"
git add backend/auth/security.py
git commit -m "feat: add bcrypt password hashing and JWT token management"
```

---

### Task 3: 创建数据模型 + 配置

**Files:**
- Create: `backend/auth/models.py`
- Modify: `backend/config.py`

- [ ] **Step 1: 创建 models.py**

```python
"""认证相关 Pydantic 模型"""
from pydantic import BaseModel


class LoginRequest(BaseModel):
    username: str
    password: str


class Token(BaseModel):
    access_token: str
    token_type: str = "bearer"


class User(BaseModel):
    username: str
    role: str


class UserInDB(User):
    password_hash: str
```

- [ ] **Step 2: 在 config.py 添加 JWT 配置**

编辑 `backend/config.py`，在 DEVICE 行之后插入：

```python
# ── JWT 认证配置 ─────────────────────────────────────
JWT_SECRET = os.getenv("JWT_SECRET", "change-me-in-production")
JWT_ALGORITHM = "HS256"
JWT_EXPIRE_MINUTES = int(os.getenv("JWT_EXPIRE_MINUTES", "480"))
```

- [ ] **Step 3: 验证**

```bash
cd C:/Users/14120/enterprise-kb-agent && D:/Anaconda3/python.exe -c "from backend.auth.models import LoginRequest, Token, User; from backend.config import JWT_SECRET; print('OK')"
```

预期：`OK`

- [ ] **Step 4: Commit**

```bash
cd "C:\Users\14120\enterprise-kb-agent"
git add backend/auth/models.py backend/config.py
git commit -m "feat: add auth Pydantic models and JWT config"
```

---

### Task 4: 创建认证依赖

**Files:**
- Create: `backend/auth/dependencies.py`

- [ ] **Step 1: 创建 dependencies.py**

```python
"""FastAPI 认证依赖注入"""
from fastapi import Depends, HTTPException, status
from fastapi.security import OAuth2PasswordBearer
from .models import User
from .database import get_connection
from .security import decode_access_token

oauth2_scheme = OAuth2PasswordBearer(tokenUrl="/api/auth/login", auto_error=False)


async def get_current_user(
    token: str | None = Depends(oauth2_scheme),
) -> User:
    """从 Bearer token 中解析当前用户，未认证返回 401"""
    if token is None:
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail="请先登录",
            headers={"WWW-Authenticate": "Bearer"},
        )
    payload = decode_access_token(token)
    if payload is None:
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail="token 无效或已过期",
            headers={"WWW-Authenticate": "Bearer"},
        )
    username = payload.get("sub")
    if username is None:
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail="token 格式错误",
        )
    conn = get_connection()
    row = conn.execute(
        "SELECT username, role FROM users WHERE username = ?", (username,)
    ).fetchone()
    conn.close()
    if row is None:
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail="用户不存在",
        )
    return User(username=row["username"], role=row["role"])


async def require_admin(
    current_user: User = Depends(get_current_user),
) -> User:
    """要求管理员角色，否则返回 403"""
    if current_user.role != "admin":
        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail="需要管理员权限",
        )
    return current_user
```

- [ ] **Step 2: 验证**

```bash
cd C:/Users/14120/enterprise-kb-agent && D:/Anaconda3/python.exe -c "from backend.auth.dependencies import get_current_user, require_admin; print('OK')"
```

预期：`OK`

- [ ] **Step 3: Commit**

```bash
cd "C:\Users\14120\enterprise-kb-agent"
git add backend/auth/dependencies.py
git commit -m "feat: add auth dependency injection (get_current_user, require_admin)"
```

---

### Task 5: 创建登录接口

**Files:**
- Create: `backend/api/auth.py`
- Modify: `backend/main.py`

- [ ] **Step 1: 创建 auth.py**

```python
"""认证 API — POST /api/auth/login"""
from fastapi import APIRouter, HTTPException, status
from ..auth.models import LoginRequest, Token
from ..auth.database import get_connection
from ..auth.security import verify_password, create_access_token

router = APIRouter(prefix="/api/auth", tags=["认证"])


@router.post("/login", response_model=Token)
def login(req: LoginRequest):
    """用户登录，返回 JWT access token"""
    conn = get_connection()
    row = conn.execute(
        "SELECT username, password_hash, role FROM users WHERE username = ?",
        (req.username,),
    ).fetchone()
    conn.close()

    if row is None or not verify_password(req.password, row["password_hash"]):
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail="用户名或密码错误",
        )

    token = create_access_token(data={"sub": row["username"], "role": row["role"]})
    return Token(access_token=token)
```

- [ ] **Step 2: 在 main.py 注册路由 + 初始化用户表**

编辑 `backend/main.py`。在现有 import 区添加：

```python
from .api.auth import router as auth_router
from .auth.database import init_admin_user
```

找到 `app.include_router(query_router)` 附近，添加：

```python
app.include_router(auth_router)
```

在 lifespan 函数中，`yield` 之前添加：

```python
init_admin_user()
```

- [ ] **Step 3: 验证路由注册**

```bash
cd C:/Users/14120/enterprise-kb-agent && D:/Anaconda3/python.exe -c "from backend.main import app; print('Routes:', [r.path for r in app.routes if 'auth' in r.path])"
```

预期：看到 `/api/auth/login` 在路由列表中。

- [ ] **Step 4: Commit**

```bash
cd "C:\Users\14120\enterprise-kb-agent"
git add backend/api/auth.py backend/main.py
git commit -m "feat: add login endpoint and admin user initialization"
```

---

### Task 6: 给查询接口添加认证依赖

**Files:**
- Modify: `backend/api/query.py`

- [ ] **Step 1: 添加认证依赖**

在 `backend/api/query.py` 顶部 import 区域添加：

```python
from ..auth.dependencies import get_current_user
```

给 `query_kb()` 函数签名添加 user 参数：

```python
@router.post("/query", response_model=QueryResponse)
async def query_kb(req: QueryRequest, user = Depends(get_current_user)):
```

给 `query_error()` 添加同样参数：

```python
@router.post("/query/error", response_model=QueryResponse)
def query_error(req: QueryRequest, user = Depends(get_current_user)):
```

给 `query_kb_stream()` 添加同样参数：

```python
@router.post("/query/stream")
async def query_kb_stream(req: QueryStreamRequest, user = Depends(get_current_user)):
```

确保文件顶部有 `Depends` import（FastAPI 自带，应该已有）。

- [ ] **Step 2: 验证语法**

```bash
cd C:/Users/14120/enterprise-kb-agent && D:/Anaconda3/python.exe -c "import ast; ast.parse(open('backend/api/query.py', encoding='utf-8').read()); print('Syntax OK')"
```

预期：`Syntax OK`

- [ ] **Step 3: Commit**

```bash
cd "C:\Users\14120\enterprise-kb-agent"
git add backend/api/query.py
git commit -m "feat: add auth requirement to query endpoints"
```

---

### Task 7: 给管理接口添加 admin 权限

**Files:**
- Modify: `backend/api/admin.py`

- [ ] **Step 1: 添加 admin 依赖**

在 `backend/api/admin.py` 顶部 import 区域添加：

```python
from ..auth.dependencies import require_admin
from ..auth.models import User
```

给 `ingest()` 函数添加 admin 参数：

```python
@router.post("/ingest", response_model=IngestResponse)
def ingest(mode: str = "incremental", admin: User = Depends(require_admin)):
```

给 `stats()` 函数添加 admin 参数：

```python
@router.get("/stats", response_model=StatsResponse)
def stats(admin: User = Depends(require_admin)):
```

`health()` 保持公开，不加认证依赖。

- [ ] **Step 2: 验证语法**

```bash
cd C:/Users/14120/enterprise-kb-agent && D:/Anaconda3/python.exe -c "import ast; ast.parse(open('backend/api/admin.py', encoding='utf-8').read()); print('Syntax OK')"
```

预期：`Syntax OK`

- [ ] **Step 3: Commit**

```bash
cd "C:\Users\14120\enterprise-kb-agent"
git add backend/api/admin.py
git commit -m "feat: add admin-only access to management endpoints"
```

---

### Task 8: 编写认证测试

**Files:**
- Create: `tests/test_auth.py`

- [ ] **Step 1: 创建 test_auth.py**

```python
"""认证模块单元测试"""
import sys
from pathlib import Path
sys.path.insert(0, str(Path(__file__).resolve().parent.parent))

import pytest
from fastapi.testclient import TestClient

# 在导入 app 之前初始化测试数据库
from backend.auth.database import create_users_table, get_connection
from backend.auth.security import hash_password
import os
os.environ["JWT_SECRET"] = "test-secret-key"

create_users_table()
conn = get_connection()
conn.execute("DELETE FROM users")  # 清空旧测试数据
conn.execute(
    "INSERT INTO users (username, password_hash, role) VALUES (?, ?, ?)",
    ("admin", hash_password("admin123"), "admin"),
)
conn.execute(
    "INSERT INTO users (username, password_hash, role) VALUES (?, ?, ?)",
    ("user1", hash_password("user123"), "user"),
)
conn.commit()
conn.close()

from backend.main import app
client = TestClient(app)


class TestAuthLogin:

    def test_login_success_returns_token(self):
        resp = client.post("/api/auth/login", json={
            "username": "admin", "password": "admin123"
        })
        assert resp.status_code == 200
        data = resp.json()
        assert "access_token" in data
        assert data["token_type"] == "bearer"

    def test_login_wrong_password_returns_401(self):
        resp = client.post("/api/auth/login", json={
            "username": "admin", "password": "wrong"
        })
        assert resp.status_code == 401

    def test_login_nonexistent_user_returns_401(self):
        resp = client.post("/api/auth/login", json={
            "username": "nobody", "password": "xxx"
        })
        assert resp.status_code == 401


class TestAuthDependencies:

    def test_no_token_returns_401(self):
        resp = client.post("/api/query", json={
            "question": "test", "mode": "knowledge_base"
        })
        assert resp.status_code == 401

    def test_invalid_token_returns_401(self):
        resp = client.post("/api/query", json={
            "question": "test", "mode": "knowledge_base"
        }, headers={"Authorization": "Bearer invalid.token.here"})
        assert resp.status_code == 401

    def test_valid_token_access_query(self):
        # 先登录
        login_resp = client.post("/api/auth/login", json={
            "username": "user1", "password": "user123"
        })
        token = login_resp.json()["access_token"]

        # 用 token 访问查询接口（期望 200，虽然可能无结果）
        resp = client.post("/api/query", json={
            "question": "test", "mode": "knowledge_base"
        }, headers={"Authorization": f"Bearer {token}"})
        assert resp.status_code == 200

    def test_normal_user_cannot_access_admin(self):
        login_resp = client.post("/api/auth/login", json={
            "username": "user1", "password": "user123"
        })
        token = login_resp.json()["access_token"]
        resp = client.get("/api/admin/stats", headers={
            "Authorization": f"Bearer {token}"
        })
        assert resp.status_code == 403

    def test_admin_can_access_admin(self):
        login_resp = client.post("/api/auth/login", json={
            "username": "admin", "password": "admin123"
        })
        token = login_resp.json()["access_token"]
        resp = client.get("/api/admin/stats", headers={
            "Authorization": f"Bearer {token}"
        })
        assert resp.status_code == 200

    def test_health_check_is_public(self):
        resp = client.get("/api/admin/health")
        assert resp.status_code == 200
```

- [ ] **Step 2: 运行测试**

```bash
cd C:/Users/14120/enterprise-kb-agent && D:/Anaconda3/python.exe -m pytest tests/test_auth.py -v
```

预期：8 passed

- [ ] **Step 3: 运行全部测试**

```bash
cd C:/Users/14120/enterprise-kb-agent && D:/Anaconda3/python.exe -m pytest tests/ -v
```

预期：18 passed（之前的 10 个 + 新的 8 个）

- [ ] **Step 4: Commit**

```bash
cd "C:\Users\14120\enterprise-kb-agent"
git add tests/test_auth.py
git commit -m "test: add authentication unit tests (8 tests)"
```

---

### Task 9: 端到端验证

- [ ] **Step 1: 启动服务并测试完整登录流程**

```bash
# 启动服务端
cd C:/Users/14120/enterprise-kb-agent && D:/Anaconda3/python.exe start.py

# 另一终端测试
curl -s -X POST http://127.0.0.1:8000/api/auth/login -H "Content-Type: application/json" -d '{"username":"admin","password":"admin123"}'
# 预期：返回 {"access_token":"...","token_type":"bearer"}

# 用 token 查询
curl -s -X POST http://127.0.0.1:8000/api/query -H "Content-Type: application/json" -H "Authorization: Bearer <token>" -d '{"question":"你好","mode":"knowledge_base"}'
# 预期：200 OK

# 无 token 查询
curl -s -X POST http://127.0.0.1:8000/api/query -H "Content-Type: application/json" -d '{"question":"你好"}'
# 预期：401 Unauthorized
```

- [ ] **Step 2: Swagger UI 验证登录**

打开 `http://localhost:8000/docs`，点击右上角 "Authorize" 按钮，输入 admin/admin123，然后测试各个接口。

- [ ] **Step 3: Final Commit**

```bash
cd "C:\Users\14120\enterprise-kb-agent"
git add .
git commit -m "chore: end-to-end auth verification"
```

---

## 文件变更总览

| 任务 | 新建 | 修改 |
|------|------|------|
| 1 | `backend/auth/__init__.py`, `database.py` | — |
| 2 | `backend/auth/security.py` | — |
| 3 | `backend/auth/models.py` | `backend/config.py` |
| 4 | `backend/auth/dependencies.py` | — |
| 5 | `backend/api/auth.py` | `backend/main.py` |
| 6 | — | `backend/api/query.py` |
| 7 | — | `backend/api/admin.py` |
| 8 | `tests/test_auth.py` | — |
| 9 | — | —（验证） |

共 **6 个新文件**，**5 个修改文件**，**8 个新测试**。
